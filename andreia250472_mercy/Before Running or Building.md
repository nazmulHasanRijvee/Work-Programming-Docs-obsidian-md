Run this command to run **build_runner** for code generation

```bash
dart run build_runner build
```

Then run this command to generate flutter apps icon from `project_root/assets/images/app_icon.png.` before running or building the app

```bash
dart run flutter_launcher_icons
```

After those commands then run:

```bash
flutter run -d emulator-255
```

