
## Packages Being Used

#### In dependencies (Will be bundled):

- **flutter_screenutil** for responsive design
- **shared_preferences** for caching
- **intl** for formatting
- **flutter_riverpod** for **Riverpod** state management
- **go_router** for navigator 2.0 advanced navigation (need to learn)
- **dio** for network calling
- **retrofit** for code generation for rest APIs (need to learn)
- **form_builder_validators** for (need to look up)
- **pinput** for entering a pin code
- **drop_button2** for (need to look up)
- **toastification** for (need to look up)
- **flutter_spinkit** for (need to look up)
- **percent_indicator** for (need to look up)
- **cached_network_image** for caching network images
- **custom_refresh_indicator** for (need to look up)
- **json_annotation** for JSON deserialization and serialization using code generation
- **google_fonts** for adding custom fonts
- **flutter_svg** for showing svg icons 
- **flutter_animate** for advanced flutter animations (need to learn)
- **skeletonizer** for showing skeleton loader (need to learn)
- **share_plus** for (need to look up)

---
#### In dev_dependencies:

- **build_runner** for running Builder and code generation
- **flutter_gen_runner** for (need to look up)
- **flutter_launcher_icons** for (need to look up)
- **flutter_lints** for flutter analyze to check code quality
- **flutter_test** for testing flutter project
- **json_serializable** for JSON deserialization and serialization, code generation
- **retrofit_generator** for code generation with dio for calling rest APIs




